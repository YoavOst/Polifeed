<div class="row">
  <div class="small-8 small-centered columns">
  <h2> המילה הכי נפוצה <h2>
    <ol>
    <% @w1 = Word.most_common(10) %>
    <% @w1.each do |w| %>
      <li> <%= "#{w.text} - #{w.amount} מופעים" %> </li>
    <% end %>          
    </ol>
  </div>
</div>
